# SPDX-FileCopyrightText: Copyright © 2026 ReallyMe LLC. All rights reserved
#
# SPDX-License-Identifier: Apache-2.0

[package]
name = "reallyme-crypto-ml-kem-768"
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = true
description = "ML-KEM-768 key encapsulation primitive for ReallyMe Crypto."

[lib]
name = "crypto_ml_kem_768"

[features]
# Native Rust PQ crypto only
default = [ ]

native = [
    "dep:ml-kem",
]

# Platform backends
wasm = [
    "dep:wasm-bindgen",
    "dep:js-sys",
]

[dependencies]
crypto-core = { package = "reallyme-crypto-core", version = "0.1.1", path = "../../core" }
zeroize = { workspace = true }

# -----------------------------
# Native-only PQ crypto deps
# -----------------------------
ml-kem = { workspace = true, features = ["getrandom", "zeroize"], optional = true }

# -----------------------------
# WASM-only deps (only if wasm/*.rs uses them)
# -----------------------------
wasm-bindgen = { workspace = true, optional = true }
js-sys = { workspace = true, optional = true }

[dev-dependencies]
hex = { workspace = true }

[lints]
workspace = true
